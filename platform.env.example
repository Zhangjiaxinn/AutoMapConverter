# Copy outside the repository, chmod 600. Token must match the Java env file.
AUTOMAP_SERVICE_TOKEN=REPLACE_WITH_THE_SAME_32_OR_MORE_CHARACTER_RANDOM_TOKEN
# Exact hostname/IP from Java app.s3.endpoint; comma-separated, no scheme/port.
AUTOMAP_STORAGE_HOSTS=10.14.86.162
