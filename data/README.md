# Data Layout

`samples/` contains compact, redistributable input maps used by examples and
regression tests. `results/` is the standard local destination for converted
maps and their diagnostic reports. Commit only small, curated artifacts.
